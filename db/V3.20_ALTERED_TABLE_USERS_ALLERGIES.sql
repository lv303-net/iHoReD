BEGIN TRANSACTION;
DROP TABLE IF EXISTS USERS_ALLERGIES;

CREATE TABLE USERS_ALLERGIES(
USER_ID INT NOT NULL FOREIGN KEY REFERENCES USERS(IDUsers),
ALLERGY_ID INT NOT NULL FOREIGN KEY REFERENCES ALLERGIES(ID_ALLERGY),
PRIMARY KEY (USER_ID, ALLERGY_ID)
);
COMMIT TRANSACTION;
